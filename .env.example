# WhalePaper does not require build-time secrets. Provider credentials are
# entered in the desktop settings and are kept in the local app profile.

# Optional: port used by `node scripts/mock-quiz-server.mjs`.
# MOCK_QUIZ_PORT=8765
